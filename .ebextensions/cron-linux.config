files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      */3 * * * * root php /var/www/html/artisan glitch:expire_events

commands:
  remove_old_cron:
  command: "rm -f /etc/cron.d/mycron.bak"